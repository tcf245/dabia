{
  "builds": [
    {
      "src": "dabia/main.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install -r requirements.txt && alembic upgrade head"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dabia/main.py"
    }
  ]
}

{
  "buildCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && /root/.cargo/bin/uv export --format requirements-txt --output-file requirements.txt && pip install -r requirements.txt && /root/.cargo/bin/uv run alembic upgrade head",
  "builds": [
    {
      "src": "dabia/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dabia/main.py"
    }
  ]
}
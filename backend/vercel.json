{
  "installCommand": "uv export --format requirements-txt --output-file requirements.txt && pip install -r requirements.txt",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "api/index.py"
    }
  ]
}